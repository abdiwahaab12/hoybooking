# Copy to .env and set your values

# MySQL (create DB first: CREATE DATABASE tailor_db;)
# Default if unset: mysql+pymysql://root:@localhost:3306/tailor_db
DATABASE_URL=mysql+pymysql://root:YOUR_PASSWORD@localhost:3306/tailor_db

# Optional: secrets (change in production)
# SECRET_KEY=your-secret-key
# JWT_SECRET_KEY=your-jwt-secret
