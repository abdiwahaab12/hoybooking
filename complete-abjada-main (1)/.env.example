# MySQL connection for tailor_db
# Copy to .env and set your values.

# Option 1: Full URL (overrides MYSQL_* below)
# DATABASE_URL=mysql+pymysql://root:yourpassword@localhost:3306/tailor_db

# Option 2: Separate variables (database defaults to tailor_db)
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=tailor_db

# Optional: secrets (change in production)
# SECRET_KEY=your-secret-key
# JWT_SECRET_KEY=your-jwt-secret
